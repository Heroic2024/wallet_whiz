<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MainProject"
        tools:targetApi="31">

        <!-- Signup Screen (Main Entry) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login Screen -->
        <activity android:name=".activity_auth" android:exported="false"/>

        <!-- Other Screens -->
        <activity android:name=".activity_budget" android:exported="false"/>
        <activity android:name=".activity_hp" android:exported="false"/>
        <activity android:name=".expense_tracker" android:exported="false"/>
        <activity android:name=".others" android:exported="false"/>
        <activity android:name=".profile" android:exported="false"/>
        <activity android:name=".activity_help" android:exported="false"/>
        <activity android:name=".contact_us" android:exported="false"/>
        <activity android:name=".activity_setting" android:exported="false"/>
        <activity android:name=".ExpenseListActivity" android:exported="false"/>

        <!-- Add DailyAnalysisActivity here -->
        <activity android:name=".DailyAnalysisActivity" android:exported="false"/>

    </application>
</manifest>
